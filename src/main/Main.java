package main;

public class Main {
    public static void main(String[] args) {
        //TODO: add switch for cli and bench
        CLI cli = new CLI();
        cli.header("0.1 - dev build");
        cli.mainMenu();
    }
}
